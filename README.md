# Vire Sabers 3D Builder

Live: https://kellanmreagan.github.io/viresabers-saberbuilder/

GitHub builds the site on every push to `main` and publishes the `gh-pages` branch. Visitors load it in a browser; nothing runs from your computer.

```bash
npm install
npm run dev
```
